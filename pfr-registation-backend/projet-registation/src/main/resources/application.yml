logging:
  file:
    name: ${user.home}/Registation/app-registation.log
  level:
    fr:
      afpa: info
    root: warn

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/registation
    username: registation_user
    password: pwd
    driver-class-name: org.mariadb.jdbc.Driver
    
  jpa:
    hibernate:      ddl-auto: update    show-sql: false
    

  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: registationcdatest@gmail.com
    password: bvrqpkjoquzrrysc #ressortir le username et password avec -D
    port: 587 #Attention port peut être fermé AFPA vers outside faire test avec partage de données 
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl.trust: smtp.gmail.com
    protocol: smtp
    test-connection: false
  
  
  
# configuration spring-mvc
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp